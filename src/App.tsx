export default function App() {
  return (
    <div style={{ padding: '2rem', fontFamily: 'var(--font-sans)' }}>
      <h1 style={{ color: 'var(--c-heading)', fontFamily: 'var(--font-display)' }}>
        ARA Design System
      </h1>
      <p style={{ color: 'var(--c-fg-muted)', marginTop: '0.5rem' }}>
        Run <code>npm run storybook</code> to view the component library.
      </p>
    </div>
  )
}
